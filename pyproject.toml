[project]
name = "calendar-bot"
version = "0.1.0"
description = "Personal Telegram calendar with persistent reminders"
requires-python = ">=3.12"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
